# SAFETY-NET.md — 리팩터 전/후 회귀 검증용 안전망 (G1)

> master.md 원칙: 정식 테스트 프레임워크가 아닌 경량 수준(배포본 브랜치 복사 + 수동 체크리스트 비교)으로 최소화.
> Phase 2 이상(B1·B2·B4·G2·D1·B3) 작업은 **착수 전 반드시 아래 절차를 따른다.**
> Phase 0 Quick Win은 이미 완료되어 이 절차 없이 진행됨 — 해당 없음.

---

## 1. 작업 전: 배포본 백업 (브랜치 복사)

터미널 없이 GitHub 웹에서만 진행 가능:

1. 저장소(`fukuoka-trip`) 페이지 → 좌측 상단 브랜치 드롭다운(`main`) 클릭
2. 입력창에 새 브랜치 이름 입력: `backup-YYYYMMDD` (예: `backup-20260725`)
3. "Create branch: backup-YYYYMMDD from 'main'" 클릭 → 현재 main이 그대로 복사됨
4. 이제부터 작업은 계속 `main`에 커밋 (백업 브랜치는 건드리지 않음)
5. 작업 완료 후 아래 체크리스트를 모두 통과하면 백업 브랜치는 그대로 두거나 삭제해도 됨
   (문제가 생기면 Settings → Branches에서 `backup-YYYYMMDD`를 다시 `main`으로 되돌리는 방식으로 롤백)

## 2. 작업 후: 수동 체크리스트

실제 배포된 링크(`https://아이디.github.io/fukuoka-trip/`)에서 아이폰/사파리로 직접 확인한다.
Bella Travel을 거쳐 들어온 경우(`?source=archive`)와 직접 링크로 들어온 경우 **둘 다** 최소 1회씩 확인 권장.

### 공통
- [ ] 페이지 로딩 정상, 콘솔 에러 없음(가능하면 확인)
- [ ] 다크모드 토글 정상 동작(테마 유지되는지 새로고침 후 재확인)
- [ ] 오프라인 상태(비행기모드)에서도 마지막으로 본 화면이 뜨는지 (Service Worker 캐시)
- [ ] Bella Travel에서 들어왔을 때만 홈(←) 버튼이 보이는지 / 직접 링크로는 안 보이는지
- [ ] 카드(모달) 열기/닫기: X 버튼, 바깥 탭, **Escape 키**(E1) 모두 정상 닫힘

### 일정 탭
- [ ] DAY1 / DAY2 / DAY3 탭 전환 정상
- [ ] 카드 탭 → 상세 모달에 메모 작성 가능
- [ ] 사진/PDF 첨부 및 삭제 정상
- [ ] 워시테이프 색상이 태그별로 정상 표시(노랑/핑크/보라/초록)

### 지도 탭
- [ ] 지도에 핀이 정상적으로 찍히는지 (day 필터 all/1/2/3)
- [ ] 핀 탭 → 하단 시트 표시, "자세히 보기"로 상세 모달 연결
- [ ] "위치 확인 필요" 목록에 뜬 곳 → "다시 시도" / "지도에서 위치 찍기" 정상 동작
- [ ] 범례(mapLegend)가 실제 사용 중인 태그만 표시하는지 (theme은 안 뜨는 게 정상 — C1)
- [ ] 오프라인 지도 타일 받기 버튼 정상 동작

### 예약함 탭
- [ ] 항공편/숙소 카드 정상 표시
- [ ] 참고정보: 공항↔시내 교통정보, 쇼핑 추천 매장, 쇼핑리스트, 꼭 먹어야 할 음식, 일본어 회화 카드 모두 정상 표시
- [ ] Visit Japan Web QR 카드가 준비물 체크리스트 하단 고정 카드로 정상 노출
- [ ] (Bella Travel 경유 시만) 가계부 카드 노출, CSV 업로드/교체 정상, 공동/개인/제외 분류 정상

### 체크리스트 탭
- [ ] 카테고리 select(여권서류/의류/전자기기/세안미용/기타/미분류) 정상
- [ ] 체크 상태가 새로고침 후에도 유지되는지(IndexedDB 저장)

### 백업/복원
- [ ] "데이터 백업 → 내보내기"로 받은 JSON에 메모·사진·체크리스트·가계부(해당 시)가 모두 포함되는지
- [ ] 그 백업 파일을 "가져오기"로 복원했을 때 원래 상태로 정확히 돌아오는지

---

## 3. 통과 기준

위 체크리스트를 모두 통과하면 해당 Phase 작업을 DONE으로 변경하고 changelog.md에 기록한다.
하나라도 실패하면 백업 브랜치 기준으로 원인 파악 후 재작업한다(Phase를 건너뛰지 않음).
